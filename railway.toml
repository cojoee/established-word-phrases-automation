[build]
builder = "dockerfile"

[deploy]
startCommand = "python main.py"
healthcheckPath = "/health"
healthcheckTimeout = 10

[env]
PYTHONUNBUFFERED = "1"

[[services]]
name = "automation"
type = "standalone"
dockerfile = "Dockerfile"
